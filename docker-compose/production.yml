web:
  build: ../web
  ports:
    - "80:80"
  links:
    - db
    - queue
  env_file: production.env
realtime:
  build: ../realtime
  ports:
    - "3000:3000"
  links:
    - queue
  env_file: production.env
db:
  image: postgres
  env_file: production.env
queue:
  image: rabbitmq
  env_file: production.env
